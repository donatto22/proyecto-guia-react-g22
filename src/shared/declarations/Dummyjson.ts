export type DummySession = {
    accessToken: string
    email: string
    firstName: string
    gender: string
    id: number
    image: string
    lastName: string
    username: string
    refreshToken: string
}

export type DummyUser = {
    username: string
    password: string
}